defaultNamespace: nginx
overrideTargets:
- clusterSelector:
     matchLabels:
        env: test
helm:
  releaseName: grafana
  chart: grafana
  repo: https://grafana.github.io/helm-charts
namespaceLabels:
  key: valueL
namespaceAnnotations: {}
labels:
  key: valuetestchangedSS2
targetCustomizations:
- name: dev
  doNotDeploy: true
  clusterSelector:
    matchLabels:
      env: test
  helm: 
    values:
      replicas: 1








