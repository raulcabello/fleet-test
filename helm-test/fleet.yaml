helm:
  releaseName: test
  chart: loki
  repo: https://grafana.github.io/helm-charts
  values:
    loki:
      storage:
        type: s3
        s3:
          s3: "s3://${S3_ACCESS_KEY}:${S3_SECRET_ACCESS_KEY}@bucketname/"

